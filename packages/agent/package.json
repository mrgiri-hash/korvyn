{
  "name": "korvyn-agent",
  "version": "0.0.1",
  "private": true,
  "description": "Real LLM agent over Korvyn's graph — the API-backed successor to the dashboard's deterministic Ask-Korvyn stub.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "agent": "tsx src/cli.ts",
    "serve": "tsx src/server.ts",
    "dryrun": "tsx src/dryrun.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.114.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
