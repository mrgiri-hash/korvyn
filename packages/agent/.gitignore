# Secrets — NEVER commit the API key
.env

# Dependencies / build
node_modules/
dist/
*.tsbuildinfo
sloane-live-eval.json
sloane-live-cross.json
sloane-live-actions.json
# Phase 1.5 measurement output — every number is re-derivable by re-running the harness, and a
# committed scorecard from one run reads as the answer rather than as one sample of a stochastic one.
sloane-v2-ab*.json
sloane-v2-transcript*.json
sloane-direct-vs-sloane*.json
sloane-concepts.json
.scratch/

# Korvyn durable work store (local development database)
data/
