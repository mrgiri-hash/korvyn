# Secrets — NEVER commit the API key
.env

# Dependencies / build
node_modules/
dist/
*.tsbuildinfo
sloane-live-eval.json
sloane-live-cross.json
sloane-live-actions.json

# Korvyn durable work store (local development database)
data/
